//
//  TabBarBuilder.swift
//  GBShop
//
//  Created by Оксана Каменчук on 19.01.2023.
//

import UIKit
/*
class TabBarBuilder {
    
    static func build() -> (UITabBarController) {
            
        let requestFactory = RequestFactory()
            
        let userViewController = UserViewAssembler.build(requestFactory: requestFactory)
        userViewController.tabBarItem = UITabBarItem(title: "Profile", image: UIImage(systemName: "person"), selectedImage: UIImage(systemName: "person.fill"))
        
        let tabBarContoller = TabBarController(controllers: [userViewController])
        
        return tabBarContoller
    }
}
*/
